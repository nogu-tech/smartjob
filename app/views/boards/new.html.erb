<div class="topic-new-wrapper" >
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">Add Project</h1>
        <%= form_for @board do |f| %>
          <div class="form-group">
            <%= f.label :tittle %>
            <%= f.text_area :tittle, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :deadline %>
            <%= f.text_area :deadline, class: 'form-control' %>
          </div>

          <%= f.submit '投稿', class: 'btn btn-black btn-block' %>

        <% end %>
      </div>
    </div>
  </div>
</div>